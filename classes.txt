GUI enable to launch the bot with different parameters for each table.

    BOT classes
        Table
            import as parent class DecisionMaking ***which is dynamically imported***
            GetFrame
            UpdatePot
            UpdateDealer
            GetHandNumber
            UpdateHandNumber (uses GetHandNumber)
            WaitForCards (Before hand is dealt)
            WaitFastFold (use same logic as WaitForCards)
            ResetAllinCount
            ResetData
            ResetStreet (counters reset when street changes)
            UpdateToCall
            UpdatePotOdds

        Player
            UpdateHole
            CheckCards
            Reset
            AllInCheck
            UpdatePossibleActions
            WaitAction
            WaitOwnAction

        DecisionMaking # Dynamicly imported Names = ["V1_0", "V1_1"]
            Default class name = DecisionMaking

    Config files
        constants.toml      # make toml to = ['constants', 'screen positions', etc]
            ['6-max'] # we decide which one to use when selecting the decision making algo
                [...]
                [...]
                [...]
                [...]

            ['9-max']
                [...]
                [...]
                [...]
                [...]
            